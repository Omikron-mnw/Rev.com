<div class="container">
  <div class="row">

    <!-- カテゴリーの新規登録フォーム -->
    <div class="">
      <%= form_with model: @category, url: admin_categories_path, method: :post, local: true do |f| %>
         <div>
           <%= f.label :"カテゴリー名" %>
         </div>
         <div>
           <%= f.text_field :category, class: "form-control" %>
         </div>
         <div>
           <%= f.submit "登録する", class: "btn btn-secondary ml-3 mt-3" %>
         </div>
      <% end %>
    </div>

    <!-- カテゴリー一覧表示 -->
    <div class="">
      <p>カテゴリー一覧</p>
      <table>
        <thead>
          <tr>
            <th>カテゴリー名</th>
          </tr>
        </thead>
        <tbody>
          <% @categories.each do |category| %>
            <tr>
              <td>
                <%= category.name %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

  </div>
</div>